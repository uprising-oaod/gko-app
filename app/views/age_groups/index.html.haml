%p#notice= notice
%h1 Age Groups
%table.table.table-striped.table-hover
  %thead
    %tr
      %th Gender
      %th Code
      %th Name
      %th Title
      %th{:colspan => "3"}
  %tbody
    - @age_groups.each do |age_group|
      %tr
        %td= age_group.gender
        %td= age_group.code
        %td= age_group.name
        %td= age_group.title
        %td= link_to 'Добавить', new_indicator_medal_path(indicator_medal: {age_group_id: age_group.id})
        %td= link_to 'Просмотр', age_group
        -#%td= link_to 'Edit', edit_age_group_path(age_group)
        -#%td= link_to 'Destroy', age_group, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Age Group', new_age_group_path
