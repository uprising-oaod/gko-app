<p id="notice"><%= notice %></p>

<h1>Age Groups</h1>

<table>
  <thead>
    <tr>
      <th>Age</th>
      <th>Gender</th>
      <th>Code</th>
      <th>Name</th>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @age_groups.each do |age_group| %>
      <tr>
        <td><%= age_group.age %></td>
        <td><%= age_group.gender %></td>
        <td><%= age_group.code %></td>
        <td><%= age_group.name %></td>
        <td><%= age_group.title %></td>
        <td><%= link_to 'Show', age_group %></td>
        <td><%= link_to 'Edit', edit_age_group_path(age_group) %></td>
        <td><%= link_to 'Destroy', age_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Age Group', new_age_group_path %>
